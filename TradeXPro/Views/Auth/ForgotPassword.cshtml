@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Password - TradeX Pro</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/auth.css" asp-append-version="true" />
    <script>
        (function() {
            var theme = localStorage.getItem('tradexpro-theme') || 'dark';
            document.documentElement.setAttribute('data-theme', theme);
        })();
    </script>
</head>
<body class="auth-body">
    <div class="auth-container auth-container-centered">
        <div class="auth-form-panel auth-form-panel-centered">
            <div class="auth-form-wrapper">
                <div class="auth-form-header" style="text-align:center;">
                    <div class="auth-logo" style="margin-bottom:24px;">TradeX Pro</div>
                    <div class="auth-reset-icon">&#128274;</div>
                    <h2 class="auth-form-title">Reset Password</h2>
                    <p class="auth-form-subtitle">Enter your email and we'll send you a link to reset your password</p>
                </div>

                <form class="auth-form">
                    <div class="auth-field">
                        <label for="email">Email Address</label>
                        <input type="email" id="email" name="email" placeholder="john@example.com" required />
                    </div>
                    <button type="submit" class="auth-submit-btn">Send Reset Link</button>
                </form>

                <div class="auth-form-footer" style="text-align:center;">
                    <p><a asp-controller="Auth" asp-action="Login" class="auth-link">&#8592; Back to Sign In</a></p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
